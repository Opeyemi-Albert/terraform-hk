#.terraform/providers/registry.terraform.io/hashicorp/aws/5.92.0/windows_amd64/terraform-provider-aws_v5.92.0_x5.exe
.terraform.lock.hcl

# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as passwords,
# private keys, and other secrets. These should not be part of version control as they are
# data points which are potentially sensitive and subject to change depending on the environment.

# Sensitive variable files
*.tfvars
*.tfvars.json     

# Ignore override files as they are usually used to override re
fxo-gxvw-rnr.terraform/


# ---- Terraform State Files ----
terraform.tfstate
terraform.tfstate.backup

# ---- override files ----
override.tf
override.tf.json
*.override.tf
*.override.tf.json

# ---- SSH Keys ----
*.pem
*.key
*.pub
id_rsa
id_rsa.*

# ---- Logs ----
*.log
